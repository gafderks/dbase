apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: media-volume
  name: media-volume
  namespace: dbase
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
